# 自动规范化换行符
* text=auto

# 明确指定代码文件在仓库中用 LF
*.js text eol=lf
*.html text eol=lf
*.css text eol=lf

# Windows 专用脚本保留 CRLF，否则可能无法运行
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件禁止转换
*.png binary
*.jpg binary
*.pdf binary